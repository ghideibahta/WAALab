import { useState } from "react";
import './ProductDetails.css';

const PostDetails = () => {

    const[selectedPost, setSelectedPost] = useState({});//empty for the detailt we want to render

    const postDetails = <div className="PostDetails">
      <div> Post Details </div>
        <h1>{ postDetails.name}</h1>
      </div>

   
}
 
export default PostDetails;